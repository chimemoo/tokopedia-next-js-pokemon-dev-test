import styled from '@emotion/styled';
import { fontSize } from '../../commons/styles/constant';

const Heading = styled.h1`
  font-family: font-bold;
  font-size: ${fontSize.heading};
  margin-bottom: 5px;
`;

export default Heading;
